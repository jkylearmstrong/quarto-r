% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/titlepage.R
\name{titlepage_pdf}
\alias{titlepage_pdf}
\title{Create a PDF title page using the nmfs-opensci/quarto_titlepages template}
\usage{
titlepage_pdf(
  output = "titlepage.pdf",
  title = NULL,
  author = NULL,
  date = NULL,
  theme = NULL,
  image = NULL,
  dir = tempfile("quarto-titlepage-"),
  quiet = FALSE,
  no_prompt = FALSE,
  ...
)
}
\arguments{
\item{output}{Character path for the output PDF. Defaults to "titlepage.pdf".}

\item{title}{Character title to place on the title page. If \code{NULL}, the
template's defaults are used.}

\item{author}{Character author(s) for the title page.}

\item{date}{Character or Date for the date to display on the title page.}

\item{theme}{Optional character theme/style to pass to the template.}

\item{image}{Optional path to an image file to include on the title/cover.
If provided the file is copied into the template directory and referenced
by basename in the generated YAML metadata.}

\item{dir}{Directory where the template and temporary files will be
installed/created. Defaults to a temporary directory created by
\code{tempfile()}; provide a persistent path to inspect or reuse the files.}

\item{quiet}{Logical; if \code{TRUE} suppresses Quarto CLI output and informational
messages where possible.}

\item{no_prompt}{Logical; if \code{TRUE} do not ask the user for interactive
approval to install the template into the current project. This is the
standard "non-interactive" override used by many R helper functions.
Defaults to \code{FALSE}; in non-interactive sessions it is automatically
treated as \code{TRUE} to avoid blocking prompts (useful in CI).}

\item{...}{Additional arguments forwarded to \code{\link[=quarto_render]{quarto_render()}} (advanced).}
}
\value{
Invisibly returns the normalized path to the generated PDF on
success. Throws an error (via cli) on failure.
}
\description{
Convenience wrapper that prepares a minimal Quarto document using the
nmfs-opensci/quarto_titlepages extension and renders a PDF title/cover
page via the Quarto CLI. The function will install the template into a
temporary directory by default, or into the current project if the user
approves installation.
}
\details{
Behavior summary:
\itemize{
\item If the template is already present under the current working directory,
it will be used without modification.
\item If not present and \code{dir} is left as the default temporary directory,
the function will prompt (interactive sessions) to install the template
into the current project. Use \code{no_prompt = TRUE} to skip prompts and
auto-install.
\item If \code{dir} is a persistent directory provided by the caller, the template
will be installed there (or re-used if already present).
}

Requirements:
\itemize{
\item Quarto command-line tool must be installed and discoverable by this R
process (see \code{\link[=find_quarto]{find_quarto()}}). Rendering to PDF may also require a LaTeX
installation depending on the template/output configuration.
}
}
\examples{
\dontrun{
# Basic usage
quarto::titlepage_pdf(output = "my-titlepage.pdf", title = "Report", author = "Me")

# Skip interactive prompts (CI or scripted use)
quarto::titlepage_pdf("my-titlepage.pdf", title = "Report", no_prompt = TRUE)

# Include an image from disk
quarto::titlepage_pdf("cover.pdf", title = "Report", image = "logo.png")
}
}
\seealso{
\itemize{
\item Template documentation: \url{https://nmfs-opensci.github.io/quarto_titlepages/}
\item \code{\link[=quarto_use_template]{quarto_use_template()}}, \code{\link[=quarto_render]{quarto_render()}}, \code{\link[=find_quarto]{find_quarto()}}
}
}
